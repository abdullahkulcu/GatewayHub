# GatewayHub — İlerleme Takibi (Faz 0)

Faz 0 kapsamı PRD Bölüm 8'e göre küçük tasklara bölünmüştür. Her çağrıda `[ ]`
durumundaki en üstteki task tamamlanır.

- [x] T1: Proje iskeleti — dizin yapısı (backend/, frontend/, worker/, docs/), Makefile (dev/test/lint), .env.example, docker-compose.yml iskeleti (postgres+redis+app+worker), README iskeleti. Backend: FastAPI app + pydantic Settings (.env okuma) + `/healthz` endpoint + pytest/ruff/mypy kurulumu.
  - `backend/app/{config.py,main.py}` + `backend/tests/test_health.py` (pytest yeşil, ruff+mypy temiz). `docker-compose.yml`'e worker servisi henüz eklenmedi — worker'ın gerçek bir davranışı olmadan (T16) compose'a eklemek yarım bırakılmış bileşen olurdu; worker `Dockerfile`'ı ve compose servisi T26'da tamamlanacak.
- [x] T2: SQLAlchemy 2 + Alembic kurulumu, DB engine/session yönetimi, Postgres bağlantısı; ilk (boş) migration.
  - `backend/app/db.py` (engine/SessionLocal/Base/get_db), `backend/alembic/` (env.py app metadata'sını okuyor, script.py.mako modern tip sözdizimiyle), `016ecf2912ae_baseline` boş migration. `backend/tests/conftest.py` her test session'ında `alembic upgrade head`'i gerçek bir test Postgres'ine (`gatewayhub_test`) karşı çalıştırıyor — testler SQLite/mock değil gerçek Postgres kullanıyor (PRD Postgres'i sabit teknoloji sayıyor).
- [x] T3: Canonical model — `users` tablosu (id, email, password_hash, role, must_change_password) + migration + testler.
  - `backend/app/models/user.py` (`UserRole` enum `admin`/`member`, DB'de `values_callable` ile küçük harf saklanıyor), migration `5eb669c2a523_add_users_table`. `tests/conftest.py`'deki `db_session` fixture'ı SQLAlchemy 2.0 `join_transaction_mode="create_savepoint"` kullanacak şekilde güncellendi — IntegrityError testinden sonra da outer transaction bozulmadan rollback edilebiliyor.
- [x] T4: Canonical model — `tasks` tablosu (PRD Bölüm 7 taslağı: id, provider, provider_task_id, parent_id, title, description, status, status_category, assignees, priority, due_date, tags, provider_raw, provider_updated_at, last_synced_at, sync_version, write_state) + migration + testler.
  - `backend/app/models/task.py`, migration `2fdf9a9f755d_add_tasks_table`. `parent_id` aynı `tasks` tablosuna self-FK (subtask ayrı varlık değil — FR-MODEL-1). `(provider, provider_task_id)` unique constraint idempotent upsert için temel. `provider_raw` JSONB ile haritalanamayan alanlar kayıpsız saklanıyor (FR-MODEL-3).
- [x] T5: Canonical model — `comments` tablosu (task_id, author, body, provider_comment_id, created_at) + migration + testler.
  - `backend/app/models/comment.py`, migration `e88b152305a4_add_comments_table`. `task_id` -> `tasks.id` `ON DELETE CASCADE`; `(task_id, provider_comment_id)` unique constraint idempotent upsert için. `author` provider'daki görünen isim (GatewayHub `users` tablosuna FK değil — yorum sahibi GatewayHub kullanıcısı olmayabilir).
- [x] T6: Canonical model — `settings` tablosu (key/value, şifreli değer) + Fernet şifreleme yardımcıları (SECRET_KEY'den anahtar türetme) + testler.
  - `backend/app/crypto.py` (`SECRET_KEY` → SHA-256 → urlsafe base64 → Fernet anahtarı; `encrypt_value`/`decrypt_value`/`mask_secret`), `backend/app/models/setting.py` (key/value + ayrı `encrypted_value` kolonu — hassas ayarlar şifreli, diğerleri düz metin). Migration `31548f31a054_add_settings_table`. `mask_secret("pk_...")` → `"pk_****"` (GOAL.md'deki maskeleme formatı).
- [x] T7: Auth — parola hashleme (argon2/bcrypt), login endpoint'i (JWT/session), ilk boot admin oluşturma (ADMIN_EMAIL/ADMIN_PASSWORD), ilk girişte parola değişikliği zorunluluğu + testler.
  - `backend/app/security.py` (argon2 hash + JWT HS256), `backend/app/bootstrap.py` (`ensure_bootstrap_admin`, FastAPI `lifespan`'da çağrılıyor), `backend/app/api/auth.py` (`POST /api/auth/login`, `POST /api/auth/change-password`, `get_current_user`/`require_active_user` dependency'leri — ikincisi `must_change_password=True` olan kullanıcıları diğer korumalı endpoint'lerden 403 ile engelliyor, T8+'da kullanılacak). Test client'ı gerçek app lifespan'ını tetiklemiyor (bootstrap'ın ayrı, rollback edilmeyen bir commit yapmaması için); bootstrap ayrıca `test_bootstrap.py`'de izole test ediliyor. `ruff`'a FastAPI'nin `Depends()` deseni için standart `extend-immutable-calls` ayarı eklendi.
- [x] T8: Kullanıcı yönetimi API'leri (admin only): ekleme/çıkarma/listeleme, parola sıfırlama + testler.
  - `backend/app/api/users.py`: `GET/POST /api/users`, `DELETE /api/users/{id}`, `POST /api/users/{id}/reset-password`; hepsi `require_admin` (role == admin + must_change_password tamamlanmış) arkasında. Yeni kullanıcılar ve parola sıfırlamaları `must_change_password=True` ile başlıyor. Son kalan admin silinemiyor (kilitlenmeyi önleme guardrail'i).
- [x] T9: `TaskProvider` abstract class (`backend/providers/base.py`): backfill(), fetch_changes(since), parse_webhook(payload) [NotImplemented], push_change(change) [NotImplemented], capabilities() + testler.
  - `ProviderTask`/`ProviderComment`/`SyncBatch` dataclass'ları adapter ile worker arasındaki normalize edilmiş (henüz DB satırı olmayan) taşıma formatı. `assignee_emails` provider kimliğini `users.email` ile eşleştiriyor (Faz 0'da ayrı bir provider-user eşleme tablosu yok — YAGNI). `ProviderCapabilities` FR-PROV-2'yi karşılıyor.
- [x] T10: ClickUp adapter — token doğrulama, workspace/space/list keşif çağrıları (respx mock'lu testler).
  - `backend/providers/clickup.py`: `ClickUpClient` (`httpx.Client`, token `Authorization` header'ında `Bearer` öneki olmadan — ClickUp'ın beklediği format), `verify_token()`, `list_workspaces/list_spaces/list_folders/list_folderless_lists/list_lists_in_folder`. Henüz `TaskProvider` alt sınıfı değil — `backfill()` gelene kadar (T11) somut adapter sınıfı açılmayacak, yarım bırakılmış abstract method'lar olmasın diye.
- [x] T11: ClickUp adapter — backfill(): task/subtask/status/assignee/tag/priority/due_date çekme ve canonical modele upsert + testler.
  - `providers/base.py`'a `StatusCategory` taşındı (adapter'ların uyması gereken sabit vocabulary — tek doğruluk kaynağı, `app/models/task.py` oradan import ediyor). `providers/clickup.py`: `ClickUpClient.list_tasks()` (sayfalama), `clickup_task_to_provider_task()` (ClickUp status.type → todo/in_progress/done eşlemesi — ClickUp'ta "cancelled" için native bir type yok, bilinçli sınırlama), `ClickUpProvider(TaskProvider)` (`fetch_changes` T13'e kadar `NotImplementedError`). `app/sync.py`: `upsert_sync_batch()` — provider-agnostic, iki geçişli parent_id çözümü (sırayla bağımsız), assignee email → `users.email` eşlemesi (bilinmeyen email'ler sessizce atlanıyor), `(provider, provider_task_id)` üzerinden idempotent upsert.
- [x] T12: ClickUp adapter — yorumların backfill'i (comments) + testler.
  - `ClickUpClient.list_comments(task_id)`, `clickup_comment_to_provider_comment()` (author: username → email → "unknown" fallback zinciri; body: `comment_text` → `comment[].text` birleştirme fallback'i). `ClickUpProvider.backfill()` artık her task için yorumları da çekip `SyncBatch.comments`'e ekliyor. `app/sync.upsert_sync_batch` yorumları `(task_id, provider_comment_id)` ile idempotent upsert ediyor; task'ı batch kapsamında bulunamayan yorumlar sessizce atlanıyor.
- [x] T13: ClickUp adapter — fetch_changes(since): artımlı sync + testler.
  - `ClickUpClient.list_tasks()` artık opsiyonel `updated_since` alıyor ve ClickUp'ın `date_updated_gt` (epoch ms) filtresini uyguluyor. `ClickUpProvider`'da `backfill()`/`fetch_changes()` ortak `_collect(updated_since)` metodunu paylaşıyor (kod tekrarı yok) — `backfill()` = `_collect(None)`, `fetch_changes(since)` = `_collect(since)`.
- [x] T14: Rate limit yönetimi — token-bucket wrapper (FR-SYNC-4) + testler.
  - `providers/rate_limit.py`: `TokenBucket.acquire()` limit dolunca hata fırlatmak yerine bekliyor (asla exception, FR-SYNC-4). `ClickUpClient` opsiyonel `rate_limiter` parametresi kabul ediyor (`_get()` her çağrıda `acquire()` çağırıyor); verilmezse hiç throttle olmuyor (mevcut testler etkilenmedi). Gerçek worker döngüsünde (T16) `ClickUpProvider.capabilities().max_rate_per_minute` ile bir `TokenBucket` kurulup client'a geçirilecek.
- [x] T15: Settings API — provider token ekleme/test etme (maskeleme `pk_****`), sync kapsamı (workspace/space/list seçimi), poll aralığı ayarı + testler.
  - `backend/app/settings_store.py`: `Setting` tablosu üzerinde tipli erişim (`clickup_api_token` şifreli, `poll_interval_seconds`/`sync_scope` düz metin/JSON). `backend/app/api/settings.py` (`require_admin` arkasında): `GET/PUT /api/settings/clickup-token` (token gerçekten ClickUp'a `verify_token()` ile doğrulanıyor, geçersizse 422), `DELETE`, `PUT /api/settings/poll-interval` (≥1 sn), `PUT /api/settings/sync-scope`, `GET /api/settings/clickup/workspaces` ve `.../lists` (klasörlü+klasörsüz listeleri tek düz listede birleştiriyor — T25'teki sync-scope seçici bunu kullanacak).
- [x] T16: Worker process — poll scheduler: her döngü başında settings'i DB'den sıcak okuma, ClickUp adapter çağırma, idempotent upsert, sync durumu kaydı (son başarılı sync, hata) + testler.
  - Asıl mantık `backend/app/poller.py`'de (`run_poll_once`/`run_forever`) — gerçek Postgres'e karşı test edilebilir olsun diye `worker/`'a değil `backend/`'e kondu. `worker/main.py` sadece ince bir process giriş noktası (backend'i `sys.path`'e ekleyip `run_forever` çağırıyor). İlk çalıştırmada `backfill()`, sonrasında kayıtlı `last_synced_at`'e göre `fetch_changes(since)` (settings tablosunda `clickup_last_synced_at`/`clickup_last_sync_error` — FR-SYNC-5). `run_poll_once` hiçbir zaman exception fırlatmıyor — hata `clickup_last_sync_error`'a yazılıyor, bir sonraki poll tekrar dener (FR-SYNC-4/NFR-2). `run_forever` her iterasyonda `poll_interval_seconds`'ı DB'den tazeden okuyor (FR-SET-2 sıcak okuma), testte doğrulandı.
- [x] T17: Tasks API — liste endpoint'i (filtreler: assignee, statü, tag, liste, parent var/yok; sıralama) + testler.
  - `backend/app/api/tasks.py`: `GET /api/tasks` (`require_active_user` arkasında — herhangi bir member erişebilir, admin şart değil). Filtreler: `assignee` (uuid, ARRAY contains), `status` (tam eşleşme — dinamik statüler FR-MODEL-4 gereği), `tag`, `list_id` (`provider_list_id`), `has_parent` (bool). `sort`/`-sort` ile sıralama, bilinmeyen anahtar sessizce `title`'a düşüyor (422 yerine). Pagination Faz 0'da yok (PRD'de talep edilmiyor — YAGNI). `assignees`/`tags` kolonları `sqlalchemy.dialects.postgresql.ARRAY`'e taşındı (`.contains()` genel `ARRAY` tipinde desteklenmiyor) — DDL değişmediği doğrulandı (autogenerate boş diff üretti), migration gerekmedi.
- [x] T18: Tasks API — detay endpoint'i (açıklama, yorumlar, subtask listesi, aktivite meta verisi) + testler.
  - `GET /api/tasks/{task_id}` (`require_active_user`): `TaskDetailOut` (`TaskOut` + `comments`/`subtasks`). `TaskOut`'a `provider_updated_at`/`sync_version` eklendi — FR-UI-4'ün "aktivite meta verisi" karşılığı (ayrı bir audit/activity-log tablosu yok, mevcut sync alanları yeterli — Faz 0 kapsamında değişiklik geçmişi tutulmuyor). Bilinmeyen task id → 404.
- [x] T19: Sync/queue durum API'si (son poll zamanı, sıradaki poll, hata durumu — FR-SYNC-5/FR-SET-4 okuma tarafı) + testler.
  - `GET /api/settings/sync-status` (`require_admin`, diğer settings endpoint'leriyle tutarlı): `last_synced_at`, `next_poll_at` (last_synced_at + poll_interval_seconds), `last_sync_error`, `poll_interval_seconds`. Bekleyen/başarısız yazma sayısı ve rate-limit durumu yok — Faz 0'da write-back queue'su yok, bu alanlar Faz 1'e ait (T19'un kendi kapsamı zaten "okuma tarafı" diyor).
- [x] T20: Frontend iskelet — Vite + React + TS + Tailwind + TanStack Query kurulumu, FastAPI static serve entegrasyonu, login sayfası + ilk girişte parola değişikliği akışı.
  - `frontend/` (Vite+React+TS+Tailwind v4+TanStack Query+React Router), `src/shared/{api,auth,authContext,useAuth}` + `AuthContext.tsx`, `LoginPage`/`ChangePasswordPage`. `backend/app/main.py` `frontend/dist`'i statik + SPA fallback olarak serve ediyor (API router'larından sonra kayıtlı, onları asla gölgelemiyor; dist yoksa tamamen atlanıyor — backend testleri etkilenmiyor). Gerçek headless tarayıcı koşusuyla uçtan uca doğrulandı (bootstrap admin girişi → zorunlu parola değişikliği → ana sayfa, konsol hatası yok). Bu sırada iki yan bulgu düzeltildi: (1) `EmailStr` `admin@local`'i reddediyordu — ayrı commit'te düzeltildi; (2) kök `.gitignore`'daki genel `lib/` kuralı `frontend/src/lib/`'i gizliyordu — dizin `src/shared/`'a taşındı.
- [x] T21: Frontend — Liste görünümü (filtrelenebilir, sıralanabilir, subtasklar bağımsız satır).
  - `TasksListPage`/`TaskFilters`/`TaskTable` — filtre state URL query param'larında (paylaşılabilir/bookmark'lanabilir), assignee/status/tag/list/parent filtreleri + tıklanabilir sütun başlıklarıyla sıralama. Subtasklar ayrı satır olarak `↳` göstergesiyle render ediliyor (nested değil, flat — FR-MODEL-1/FR-UI-1). Gerçek headless tarayıcı koşusuyla doğrulandı: filtre ve sıralama gerçek verilerle doğru çalışıyor, konsol hatası yok. `main.tsx`'e global 401 handling eklendi (token geçersizse otomatik login'e döner).
- [x] T22: Frontend — Board görünümü (statü kolonlu, subtask bağımsız kart + parent rozeti, aç/kapa toggle).
  - `BoardPage`/`BoardColumn`/`TaskCard` — kolonlar `status_category` sırasına göre (todo→in_progress→done→cancelled, bilinmeyenler sona) dizilmiş dinamik `status` değerleri. "Show subtasks as cards" toggle'ı kapatıldığında sadece üst seviye tasklar gösteriliyor; açıkken subtasklar kendi statü kolonunda bağımsız kart olarak render ediliyor ve kartta parent'a küçük "↳ <parent title>" rozeti var (parent farklı bir kolonda olsa bile). **Kapsam dışı bırakılan:** sürükle-bırak ile statü değiştirme — bu bir yazma (write-back) eylemi, Faz 0'da yok ve GOAL.md'nin "temelini bile atma" kuralı gereği eklenmedi; board şu an salt-okunur bir görselleştirme. Gerçek tarayıcı koşusuyla doğrulandı.
- [x] T23: Frontend — Task detay paneli (markdown açıklama, yorumlar, subtask listesi, aktivite meta verisi).
  - `TaskDetailPanel` — sağdan açılan slide-over panel (backdrop tıklaması kapatır), liste ve board görünümünden ortak `useSelectedTask` hook'u (`?task=<id>` URL param'ı — paylaşılabilir/geri tuşuyla uyumlu) ile açılıyor. `react-markdown` + `@tailwindcss/typography` ile açıklama render ediliyor, subtask listesi ve yorumlar (`GET /api/tasks/{id}`'den), aktivite bölümünde write_state/last_synced_at/provider_updated_at/sync_version. Gerçek tarayıcı koşusuyla doğrulandı: markdown (h2 + bold + liste), subtask, yorum ve aktivite verisi hepsi doğru render ediliyor, backdrop tıklaması paneli kapatıyor.
- [ ] T24: Frontend — Keyboard-first navigasyon (j/k, statü/assignee hızlı değişim, command palette Cmd/Ctrl+K).
- [ ] T25: Frontend — Settings sayfası (provider token, sync kapsamı, poll aralığı, kullanıcı yönetimi UI).
- [ ] T26: Docker Compose — tam entegrasyon (app: API+UI static serve, worker, postgres, redis) tek komutla ayağa kalkma; Dockerfile'lar.
- [x] T20b: `docs/INTEGRATIONS.md` — kullanıcı isteği: ClickUp (Faz 0'da mevcut) entegrasyonunun nasıl kurulacağını anlatan doküman (Personal API Token nasıl alınır, hangi workspace/space/list ID'leri gerekli, panelde nereye girilir). Jira, Faz 2'ye kadar mevcut değil — dokümanda bu açıkça belirtilir, hayali adımlar yazılmaz.
  - Settings UI (T25) henüz yok, bu yüzden doküman gerçek `/api/settings/*` endpoint'lerini `curl` örnekleriyle anlatıyor — var olmayan bir panel akışını anlatmıyor. T25 tamamlandığında doküman panel adımlarına güncellenmeli.
- [ ] T27: `setup.sh` — kullanıcı isteği: makineye indirip tek script ile kurulum (`.env` yoksa `.env.example`'dan oluştur + rastgele `SECRET_KEY` üret + `docker compose up -d --build` + hazır olduğunda panel URL'ini yazdır). README'deki `cp .env.example .env && docker compose up` akışının yerini almaz, ona ek pratik bir kısayoldur.
- [ ] T28: README finalize (≤10 satır kurulum, `setup.sh` seçeneğine referans), Faz 0 kapsam kontrolü.
- [ ] T29: Kalite geçişi — ruff+mypy+tsc temiz, coverage ≥%80 core+provider, tüm testler yeşil; Faz 0 başarı kriterleri kontrolü.
